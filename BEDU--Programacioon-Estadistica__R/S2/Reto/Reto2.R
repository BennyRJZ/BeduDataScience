# Reto 2

# Estructura Interna de un Objeto
str(mtcars)

#Obtiene resumenes estadisticos de los objetos MINIMO, 1Q, Mediana, Media, 3Q, MAX
summary(mtcars)

#Obtiene la 1a parte de un Objeto
head(mtcars)

#Visualiza al Objeto
View(mtcars)